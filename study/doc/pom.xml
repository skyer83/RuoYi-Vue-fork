<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.ruoyi</groupId>
        <artifactId>ruoyi</artifactId>
        <version>3.9.2</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>doc</artifactId>

    <name>Maven</name>
    <url>http://maven.apache.org/</url>
    <inceptionYear>2001</inceptionYear>

    <distributionManagement>
        <site>
            <id>website</id>
            <url>scp://webhost.company.com/www/website</url>
        </site>
    </distributionManagement>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    </properties>

    <build>
        <plugins>
            <!-- Maven Site 插件（基础） -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-site-plugin</artifactId>
                <version>3.12.1</version>
                <configuration>
                    <!-- 支持的语言：中文（zh）、英文（默认）、法文（fr） -->
                    <locales>en,fr</locales>
                    <!-- 注意：defaultLocale 和 skin 参数在 3.12.1 已移除，默认使用 locales 第一个语言 -->
                </configuration>
                <dependencies>
                    <!-- 支持 Markdown 格式 -->
                    <dependency>
                        <groupId>org.apache.maven.doxia</groupId>
                        <artifactId>doxia-module-markdown</artifactId>
                        <version>1.12.0</version>
                    </dependency>
                </dependencies>
            </plugin>

            <!--
                在 site 构建后注入 JavaScript，支持拖拽调整侧边栏宽度和菜单折叠
            -->
            <plugin>
                <groupId>com.google.code.maven-replacer-plugin</groupId>
                <artifactId>replacer</artifactId>
                <version>1.5.3</version>
                <executions>
                    <execution>
                        <id>add-javascript</id>
                        <phase>site</phase>
                        <goals>
                            <goal>replace</goal>
                        </goals>
                        <configuration>
                            <basedir>${project.build.directory}/site</basedir>
                            <includes>
                                <include>**/*.html</include>
                            </includes>
                            <replacements>
                                <replacement>
                                    <token><![CDATA[</body>]]></token>
                                    <value><![CDATA[<script>(function(){document.addEventListener('DOMContentLoaded',function(){var l=document.getElementById('leftColumn'),b=document.getElementById('bodyColumn');if(!l||!b)return;var r=document.createElement('div');r.id='menuResizer';r.style.cssText='position:absolute;right:0;top:0;bottom:0;width:5px;cursor:col-resize;background:transparent;z-index:1000';l.style.position='relative';l.appendChild(r);var d=false,sx=0,sw=0;r.addEventListener('mousedown',function(e){d=true;sx=e.clientX;sw=l.offsetWidth;document.body.style.cursor='col-resize';document.body.style.userSelect='none';e.preventDefault()});document.addEventListener('mousemove',function(e){if(!d)return;var w=Math.max(180,Math.min(500,sw+(e.clientX-sx)));l.style.width=w+'px';l.style.maxWidth=w+'px';b.style.marginLeft=(w+20)+'px';localStorage.setItem('menuWidth',w)});document.addEventListener('mouseup',function(){if(d){d=false;document.body.style.cursor='';document.body.style.userSelect=''}});r.addEventListener('mouseenter',function(){this.style.background='#ccc'});r.addEventListener('mouseleave',function(){if(!d)this.style.background='transparent'});var w=localStorage.getItem('menuWidth');if(w){w=parseInt(w);l.style.width=w+'px';l.style.maxWidth=w+'px';b.style.marginLeft=(w+20)+'px'}var expandedItems=JSON.parse(localStorage.getItem('menuExpanded')||'{}');document.querySelectorAll('#navcolumn li').forEach(function(item){var ul=item.querySelector(':scope > ul');if(!ul)return;var link=item.querySelector(':scope > a')||item.querySelector(':scope > strong');if(!link)return;var href=link.getAttribute('href')||'';var key=href||link.textContent;if(!item.classList.contains('expanded')&&!item.classList.contains('collapsed')){item.classList.add('expanded')}if(expandedItems[key]===false){item.classList.remove('expanded');item.classList.add('collapsed')}item.addEventListener('click',function(e){if(e.target!==item)return;e.preventDefault();e.stopPropagation();item.classList.toggle('expanded');item.classList.toggle('collapsed');expandedItems=JSON.parse(localStorage.getItem('menuExpanded')||'{}');expandedItems[key]=item.classList.contains('expanded');localStorage.setItem('menuExpanded',JSON.stringify(expandedItems))})})})})();</script></body>]]></value>
                                </replacement>
                            </replacements>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
